<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="mobi.ccjr.ptel.MainActivity">

    <fragment
        android:id="@+id/send_fragment"
        android:name="mobi.ccjr.ptel.fragment.SendFragment"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <TextView
        android:id="@+id/recent_balance"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/send_fragment"
        android:gravity="center"
        android:padding="@dimen/large_padding"
        android:text="kaboom"
        android:textAppearance="@style/HugeTextStyle" />

    <TextView
        android:id="@+id/recent_expiry"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/recent_balance"
        android:gravity="center"
        android:padding="@dimen/large_padding"
        android:text="zoooom"
        android:textAppearance="@style/BigTextStyle" />

</RelativeLayout>